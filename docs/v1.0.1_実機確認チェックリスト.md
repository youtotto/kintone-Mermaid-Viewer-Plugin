# Mermaid Viewer v1.0.1 hotfix 実機確認チェックリスト

作成日: 2026-09-15
対象コード: main f43006a + 未コミット変更（source/desktop.js, source/config.js）
確認用 ZIP: `build/Mermaid-Viewer-Plugin-v1.0.1-test.zip`（manifest version は 1.0.0 のまま。アイコンは現正式版と同一）
ローカルテスト: 50 PASS / 0 FAIL

各項目の「結果」に PASS / FAIL / 未確認 を記入する。FAIL は末尾の「FAIL 記録」に書く。

> **注意**: プラグインの読み込み・更新は kintone 環境全体に効く。本番アプリが同じ環境でこのプラグインを使っている場合、確認用 ZIP に更新した時点で本番も更新版で動く。開発用環境での実施を推奨。戻す場合は現正式 ZIP（`Mermaid-Viewer-Plugin.zip`、v1.0.0）を再読み込みする。
> バージョン表示はどちらも 1.0.0。判別はツールバーに「全体表示／−／＋」があるかどうかで行う（ある＝確認用 ZIP）。

## 優先順位

最優先: A 構文エラー独立化 / D strict / E・G ズーム・全体表示 / K 既存設定保持
次点: H 全画面 / J GROUP / I CDN障害 / L 正常系

---

## 0. 事前準備

### 0-1. 検証アプリのフォーム

| フィールド名 | フィールドコード | 種類 | 用途 |
|---|---|---|---|
| 図1 | `図1` | 文字列（複数行） | A（正常） |
| 図2 | `図2` | 文字列（複数行） | A（構文エラー） |
| 図3 | `図3` | 文字列（複数行） | A（正常） |
| 図4 | `図4` | 文字列（複数行） | K（後で削除する） |
| スペース1〜4 | `space1`〜`space4` | スペース（要素ID を設定） | 描画先。space4 は K で後で削除 |
| グループ | `グループ` | グループ（内に `space_in_group`） | J |
| （任意）ネストグループ | 内に `space_nested` | J |

### 0-2. テスト用 Mermaid コード

```
[正常A] flowchart
flowchart TD
  A[開始] --> B{判定}
  B -->|Yes| C[完了]
  B -->|No| A

[構文エラー]
graph TD
  A -->
  B ==>

[正常B] sequence
sequenceDiagram
  autonumber
  User->>App: 登録
  App-->>User: OK

[D 用] classDiagram
classDiagram
  class Animal {
    +String name
    +speak()
  }
  Animal <|-- Dog

[D 用] stateDiagram-v2
stateDiagram-v2
  [*] --> Idle
  Idle --> Running : start
  Running --> [*]

[D 用] erDiagram
erDiagram
  CUSTOMER ||--o{ ORDER : places
  ORDER ||--|{ LINE_ITEM : contains

[D 用] click 付き（JS が実行されないこと）
flowchart LR
  A[押してみる] --> B[OK]
  click A call alert("xss")
```

```
[ ] 0-1 確認用 ZIP を kintone システム管理 → プラグイン → 読み込む で「更新」として読み込めた（一覧に 1 件のまま）  結果: (      )
[ ] 0-2 検証アプリの設定でマッピング 図1→space1、図2→space2、図3→space3 を保存、高さ 500・余白 30            結果: (      )
[ ] 0-3 詳細画面のツールバーに「全体表示 − ＋ 全画面」が表示される（確認用 ZIP が有効になっている判定）        結果: (      )
```

ブラウザの開発者ツール（F12）Console を開いた状態で以下を実施する。

---

## A. 構文エラー独立化（最優先）

レコードに 図1=[正常A]、図2=[構文エラー]、図3=[正常B] を入力して保存し、詳細画面を開く。

```
[ ] A-1 1件目（space1）に flowchart が描画される                                    結果: (      )
[ ] A-2 2件目（space2）だけがエラー表示になる                                       結果: (      )
[ ] A-3 3件目（space3）に sequence 図が描画される（2件目で止まらない）               結果: (      )
[ ] A-4 順番を入れ替え 図1 を構文エラーにしても 2・3 件目は描画される                 結果: (      )
[ ] A-5 Console に赤字の Uncaught エラーが出ない（console.error の詳細ログは出てよい） 結果: (      )
```

## B. エラー表示

```
[ ] B-1 エラー枠に「Mermaid図を表示できませんでした。構文を確認してください。」        結果: (      )
[ ] B-2 補足行に「N行目付近：「…」の付近に問題があります」のような短い説明が出る          結果: (      )
[ ] B-3 スタックトレースや英語の長文（Expecting … got …）が画面に出ない                結果: (      )
[ ] B-4 エラー枠のボタン（全体表示／−／＋／全画面）が無効（グレー）になっている           結果: (      )
[ ] B-5 空欄のフィールドは「Mermaidコードが未入力です。」                               結果: (      )
```

## C. Mermaid 一時 DOM

```
[ ] C-1 A の状態でページ最下部までスクロールしても、爆弾アイコンや「Syntax error in text」が無い     結果: (      )
[ ] C-2 Console で document.querySelectorAll('[id^="dnrmermaid_"], [id^="dmermaid"]').length が 0   結果: (      )
```

## D. strict mode（最優先）

図1〜3 に順に入れて確認（または 1 図ずつ差し替え）。

```
[ ] D-1 flowchart が描画される                                                        結果: (      )
[ ] D-2 sequenceDiagram が描画される                                                  結果: (      )
[ ] D-3 classDiagram が描画される                                                     結果: (      )
[ ] D-4 stateDiagram-v2 が描画される                                                  結果: (      )
[ ] D-5 erDiagram が描画される                                                        結果: (      )
[ ] D-6 click 付きの図: ノード「押してみる」をクリックしても alert が出ない（図は表示される）  結果: (      )
[ ] D-7 Console で mermaid.mermaidAPI.getConfig().securityLevel が "strict"            結果: (      )
```

## E. ホイールズーム（最優先）

```
[ ] E-1 図の上でホイール 1 ノッチ上 → 少し拡大する（従来の約 1.4 倍より控えめ、約 1.12 倍）          結果: (      )
[ ] E-2 1 ノッチで 2 段階飛ぶような不自然な拡大にならない                                       結果: (      )
[ ] E-3 ホイール下で縮小する。連続操作で最小約 0.5 倍・最大約 8 倍で止まる                       結果: (      )
```

## F. ダブルクリック

```
[ ] F-1 図をダブルクリックしても拡大しない                                              結果: (      )
```

## G. Viewer 操作（最優先）

```
[ ] G-1 「＋」で拡大する                                                                 結果: (      )
[ ] G-2 「−」で縮小する                                                                  結果: (      )
[ ] G-3 ドラッグで図を枠外へ移動し、ホイールで拡大したあと「全体表示」で最初の表示に戻る       結果: (      )
[ ] G-4 全体表示直後の見え方が、詳細画面を開いた直後と同じ                                   結果: (      )
[ ] G-5 ドラッグ（パン）が従来どおり動く                                                  結果: (      )
```

## H. 全画面（次点）

```
[ ] H-1 「全画面」で全画面表示が開き、図が全体表示で出る                                   結果: (      )
[ ] H-2 全画面内で ＋ / − / 全体表示 / ドラッグ / ホイールが動く                              結果: (      )
[ ] H-3 「閉じる」で戻る                                                                  結果: (      )
[ ] H-4 Esc キーで戻る                                                                    結果: (      )
[ ] H-5 暗い背景部分のクリックで戻る                                                       結果: (      )
[ ] H-6 閉じたあとページがスクロールできる（body の overflow が戻っている）                  結果: (      )
[ ] H-7 構文エラーの枠では「全画面」が無効になっている                                       結果: (      )
```

## I. CDN 障害（次点）

DevTools → Network → Offline にしてから詳細画面をリロード（オンラインに戻すのを忘れない）。

```
[ ] I-1 各枠に「読み込み中…」→ 20 秒以内に「図の表示に必要なライブラリを読み込めませんでした。」   結果: (      )
[ ] I-2 永久に「読み込み中…」のままにならない                                                 結果: (      )
[ ] I-3 Console に同じログが延々と出続けない（読み込み失敗の console.error が数件出るのは正常）  結果: (      )
[ ] I-4 Online に戻してリロードすると通常どおり描画される                                       結果: (      )
[ ] I-5 （可能なら）panzoom の URL だけをブロック → 図は表示され、全体表示／−／＋が無効、全画面は使える  結果: (      )
```

## J. GROUP 内スペース（次点）

```
[ ] J-1 グループ内に置いたスペース（space_in_group）が設定画面の描画先候補に出る            結果: (      )
[ ] J-2 （可能なら）ネストしたグループ内のスペース（space_nested）も候補に出る               結果: (      )
[ ] J-3 グループ内スペースをマッピングして保存 → 詳細画面でその位置に図が描画される            結果: (      )
[ ] J-4 テーブル内の要素は候補に出ない（テーブルにはスペースを置けないので、候補が増えていないこと） 結果: (      )
```

## K. 既存設定保持（最優先）

1. 設定画面で 図4→space4 のマッピングを追加し、高さ `600`・余白 `20` にして保存する（合計 4 件）。
2. フォーム設定で「図4」フィールドと「space4」スペースを削除して、アプリを更新する。
3. プラグイン設定画面を開く。

```
[ ] K-1 設定画面がエラーなく開き、マッピングが 4 件表示される                                   結果: (      )
[ ] K-2 4 件目のフィールドが「図4（現在使用できないフィールド）」表示                              結果: (      )
[ ] K-3 4 件目のスペースが「space4（現在使用できないスペース）」表示                               結果: (      )
[ ] K-4 高さ 600・余白 20 が表示されている                                                      結果: (      )
[ ] K-5 何も変更せず「保存」→ エラーなく保存完了                                                 結果: (      )
[ ] K-6 再度開いて field / space / 高さ / 余白 / 件数（4）が変わっていない                        結果: (      )
[ ] K-7 詳細画面を開くと 図1〜3 は描画され、4 件目は Console の warn だけで画面は壊れない            結果: (      )
[ ] K-8 4 件目の行を「削除」して保存 → 3 件になり、他の 3 件は不変                                 結果: (      )
```

## L. 既存正常系（次点）

```
[ ] L-1 単一マッピング（1 件だけ）で描画される                                              結果: (      )
[ ] L-2 複数マッピングで各枠が独立して pan / zoom できる（片方を拡大しても他方は変わらない）    結果: (      )
[ ] L-3 pan（ドラッグ）                                                                   結果: (      )
[ ] L-4 zoom（ホイール）                                                                  結果: (      )
[ ] L-5 全画面の開閉                                                                       結果: (      )
[ ] L-6 設定保存後にプラグイン一覧へ戻り、保存メッセージが出る                               結果: (      )
[ ] L-7 同じスペースを 2 行に割り当てて保存 → 警告が出て保存されない                          結果: (      )
[ ] L-8 同じフィールドを別スペースに割り当てるのは保存できる                                 結果: (      )
```

## 6. 後片付け

```
[ ] 6-1 DevTools の Offline を解除した                                                        結果: (      )
[ ] 6-2 検証用マッピング・レコードを整理した                                                 結果: (      )
[ ] 6-3 環境を戻す場合: Mermaid-Viewer-Plugin.zip（v1.0.0）を「更新」で再読み込み（ツールバーの全体表示ボタンが消えれば戻っている）  結果: (      )
```

---

## 結果サマリ

| 区分 | 件数 | PASS | FAIL | 未確認 |
|---|---|---|---|---|
| 0 事前準備 | 3 | | | |
| A 構文エラー独立化 | 5 | | | |
| B エラー表示 | 5 | | | |
| C 一時 DOM | 2 | | | |
| D strict | 7 | | | |
| E ホイール | 3 | | | |
| F ダブルクリック | 1 | | | |
| G Viewer 操作 | 5 | | | |
| H 全画面 | 7 | | | |
| I CDN 障害 | 5 | | | |
| J GROUP | 4 | | | |
| K 既存設定保持 | 8 | | | |
| L 正常系 | 8 | | | |
| 後片付け | 3 | | | |

## FAIL 記録

```
項目:
操作:
期待結果:
実際の結果:
Console:
再現性:   ( 毎回 / 時々 / 1回のみ )
```

```
項目:
操作:
期待結果:
実際の結果:
Console:
再現性:   ( 毎回 / 時々 / 1回のみ )
```
